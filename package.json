{
  "name": "logic-training-platform",
  "version": "1.0.0",
  "description": "逻辑思维训练平台 - 全栈项目",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start": "start.bat",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "node-windows": "^1.0.0-beta.8"
  }
}
